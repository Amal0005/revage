<%- include("../../views/partials/user/header") %>
import React from 'react';

const ElegantOrderTracker = () => {
  const orders = [
    {
      id: "ORD123456",
      date: "January 10, 2025",
      status: "Processing",
      items: [
        {
          name: "Classic White Shirt",
          quantity: 2,
          price: 2499,
          image: "/api/placeholder/80/80"
        }
      ],
      total: 4998
    }
  ];

  return (
    <div className="max-w-6xl mx-auto p-8 font-serif">
      {/* Elegant Header */}
      <h1 className="text-4xl font-light text-black mb-12 text-center border-b border-black pb-4 tracking-widest">
        ORDER HISTORY
      </h1>

      {orders.length > 0 ? (
        <div className="space-y-8">
          {orders.map((order) => (
            <div key={order.id} className="group">
              <div className="border border-black p-6 bg-white transition-all duration-300 hover:shadow-2xl relative">
                {/* Decorative Element */}
                <div className="absolute top-0 left-0 w-1 h-full bg-black" />
                
                {/* Order Header */}
                <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8 border-b border-gray-200 pb-6">
                  <div className="space-y-1">
                    <p className="text-xs uppercase tracking-widest text-gray-500">Order Number</p>
                    <p className="font-light text-lg">{order.id}</p>
                  </div>
                  <div className="space-y-1">
                    <p className="text-xs uppercase tracking-widest text-gray-500">Date Placed</p>
                    <p className="font-light text-lg">{order.date}</p>
                  </div>
                  <div className="space-y-1">
                    <p className="text-xs uppercase tracking-widest text-gray-500">Status</p>
                    <span className="inline-block px-4 py-1 border border-black text-xs uppercase tracking-wider">
                      {order.status}
                    </span>
                  </div>
                </div>

                {/* Order Items */}
                <div className="space-y-6">
                  {order.items.map((item, idx) => (
                    <div key={idx} className="flex items-center space-x-6 p-4 bg-gray-50">
                      <img
                        src={item.image}
                        alt={item.name}
                        className="w-20 h-20 object-cover border border-black"
                      />
                      <div className="flex-1">
                        <h3 className="text-lg font-light mb-2">{item.name}</h3>
                        <p className="text-sm text-gray-600">Quantity: {item.quantity}</p>
                        <p className="text-sm">₹{item.price.toLocaleString('en-IN')}</p>
                      </div>
                    </div>
                  ))}
                </div>

                {/* Order Footer */}
                <div className="mt-6 pt-6 border-t border-gray-200 flex justify-between items-center">
                  <p className="text-xl font-light">
                    Total: ₹{order.total.toLocaleString('en-IN')}
                  </p>
                  <div className="space-x-4">
                    <button className="px-6 py-2 border-2 border-black text-black hover:bg-black hover:text-white transition-colors duration-300 text-sm uppercase tracking-wider">
                      View Details
                    </button>
                    <button className="px-6 py-2 bg-black text-white border-2 border-black hover:bg-white hover:text-black transition-colors duration-300 text-sm uppercase tracking-wider">
                      Track Order
                    </button>
                  </div>
                </div>
              </div>
            </div>
          ))}
        </div>
      ) : (
        <div className="text-center py-16 border border-black">
          <h3 className="text-2xl font-light mb-4">No Orders Yet</h3>
          <p className="text-gray-600 mb-8">Your order history is currently empty</p>
          <button className="px-8 py-3 bg-black text-white hover:bg-gray-900 transition-colors duration-300 uppercase tracking-wider text-sm">
            Start Shopping
          </button>
        </div>
      )}
    </div>
  );
};

export default ElegantOrderTracker;

<script>
async function cancelOrder(orderId) {
    if (!confirm('Are you sure you want to cancel this order?')) {
        return;
    }

    try {
        const response = await fetch(`/orders/${orderId}/cancel`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            }
        });

        const data = await response.json();

        if (response.ok) {
            Swal.fire({
                icon: 'success',
                title: 'Success!',
                text: data.message,
                showConfirmButton: false,
                timer: 1500
            }).then(() => {
                window.location.reload();
            });
        } else {
            throw new Error(data.message);
        }
    } catch (error) {
        Swal.fire({
            icon: 'error',
            title: 'Oops...',
            text: error.message || 'Failed to cancel order'
        });
    }
}
</script>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<%- include("../../views/partials/user/footer") %>
